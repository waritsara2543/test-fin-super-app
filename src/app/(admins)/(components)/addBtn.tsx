"use client";
import { Button } from "@mui/material";
import React from "react";

const AddBtn = () => {
  return (
    <Button variant="contained" style={{ backgroundColor: "#1e3a8a" }}>
      Add
    </Button>
  );
};

export default AddBtn;
